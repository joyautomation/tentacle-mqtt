# MQTT Sparkplug B Configuration
# ==============================

# MQTT Broker URL (required)
# Supports mqtt:// for plain TCP and mqtts:// for TLS
MQTT_BROKER_URL=mqtt://localhost:1883

# MQTT Client ID (optional, auto-generated if not provided)
MQTT_CLIENT_ID=tentacle-mqtt-bridge

# MQTT Sparkplug Group ID (optional, default: TentacleGroup)
MQTT_GROUP_ID=TentacleGroup

# MQTT Sparkplug Edge Node (optional, default: EdgeNode)
MQTT_EDGE_NODE=EdgeNode1

# MQTT Authentication (optional)
MQTT_USERNAME=
MQTT_PASSWORD=

# MQTT Keep-alive interval in seconds (optional, default: 30)
MQTT_KEEPALIVE=30

# MQTT TLS Configuration (only for mqtts://, optional)
# MQTT_TLS_CERT_PATH=/path/to/client-cert.pem
# MQTT_TLS_KEY_PATH=/path/to/client-key.pem
# MQTT_TLS_CA_PATH=/path/to/ca-cert.pem

# MQTT Device ID (optional)
# If set, will use DDATA (Device Data) instead of NDATA (Node Data)
# MQTT_DEVICE_ID=device-001

# NATS Configuration
# ==================

# NATS Servers (optional, default: localhost:4222)
# Comma-separated for multiple servers
NATS_SERVERS=localhost:4222

# NATS Authentication (optional)
NATS_USER=
NATS_PASS=

# NATS Token Authentication (alternative to username/password)
# NATS_TOKEN=

# Project Configuration
# =====================

# Project ID (required)
# Used for NATS topic naming: plc.data.{projectId}.{variableId}
# And NATS KV bucket naming: plc-variables-{projectId}
PROJECT_ID=my-plc-project

# Example configurations for different scenarios:
# ===============================================

# Local development (plain MQTT)
# MQTT_BROKER_URL=mqtt://localhost:1883
# NATS_SERVERS=localhost:4222
# PROJECT_ID=my-plc-project

# Production with TLS and authentication
# MQTT_BROKER_URL=mqtts://mqtt.example.com:8883
# MQTT_USERNAME=bridge_user
# MQTT_PASSWORD=secure_password
# MQTT_TLS_CA_PATH=/etc/ssl/certs/ca-bundle.crt
# NATS_SERVERS=nats.example.com:4222
# NATS_USER=nats_user
# NATS_PASS=nats_password
# PROJECT_ID=production-plant

# Multiple NATS servers for high availability
# NATS_SERVERS=nats1.example.com:4222,nats2.example.com:4222,nats3.example.com:4222
# NATS_USER=ha_user
# NATS_PASS=ha_password
# PROJECT_ID=ha-system
